'use strict';
// import libraries & generated/ts

(async () => {

    // start peer node

    // register EtherRPC ServiceDef

    // register Fluence.getStatus().peerId to Router

    // run https://web3js.readthedocs.io/en/v1.2.0/web3-eth.html#eth-sendsignedtransaction

})();